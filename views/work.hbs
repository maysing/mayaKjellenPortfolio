<h1>{{work.title}}</h1>
{{#if work}}
	<p> {{work.link}}</p>
    <p> {{work.course}}</p>
    <p> {{work.year}}</p>
    
    <form action="/delete-work/{{work.id}}" method="POST">
        <input type="submit" value="DELETE">
    </form>

{{else}}
	<p>That work do not exist.</p>
{{/if}}

