<h1>Works</h1>


{{#if works.length}}
    <ul id="worksList">
        {{#each works}}
            <li> <a href="/works/{{id}}">{{id}} {{title}} </a></li>
        {{/each}}
    </ul>

{{else}}

    <p>At the moment we have no works to display</p>

{{/if}}


